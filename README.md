# sentiment-analysis-ml-flask
A simple sentiment analysis web application built using Flask and a machine learning model (Logistic Regression with TF-IDF) to classify text. The model was trained on a dataset consisting of tweets, where each tweet is labeled as positive, neutral, or negative.
